# Copy to .env and fill in. .env is gitignored — never commit the real key.
ANTHROPIC_API_KEY=sk-ant-...

# Optional but recommended: pin CORS to your extension.
# chrome://extensions shows the ID under the extension name.
EXTENSION_ID=

PORT=3111
